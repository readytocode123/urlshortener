<p> To prevent any bugs that prevent you from using our amazing software, please include the full url INCLUDING the protocol you are using, e.g.
"http://www.google.com" </p> 
<!--              this url is a rails thing! -->
<%= form_for @url, url: url_create_path do |f| %>
<!--             this one is not a rails thing this one refers to the url in schema-->
<%= f.text_field :url %>
<%= f.submit %>

<% end %>

 <% if params[:id] %>
 <%= url_url(params[:id]) %>
 <% end %>
